Issue-236 M1-A CI trigger.

Only update this file after the runner, workflow, physics code, and generated-input contract are all ready for an intentional CI run.
Changing experiment/helper/source files alone must not trigger the Issue-236 smoke workflow.

Intentional run: disable volumetric electron chemistry and volumetric electron-energy source/sink kernels while keeping electron/energy wall losses, SEE, live electron transport, Poisson coupling, and frozen heavy-transport electron density.

Intentional run: validate canonical electron-energy electrostatic drift restoration with dt_e=1e-10 s, dt_h=1e-8 s, 100 fast electron+energy+Poisson substeps per heavy step, and total time 2e-8 s.

Intentional diagnostic run: record recovery-safe parallel A/B/C/D evidence for accepted child state, same-event child-to-parent operational vs snapshot transfer, parent heavy nonlinear state, and the exact heavy_transport electron-density functor. Parent attempts are keyed by timestep, failed-step count, dt, and time; accepted lanes exclude FAILED output.

Intentional rerun: execute the unchanged A/B/C/D discriminator after adding the host-side Python syntax gate and retaining the pinned-container self-test. No physics, timestep, transfer payload, or diagnostic-lane semantics are changed.

Intentional boundary-face A/B run: keep the transferred child state, timestep policy, chemistry state, MultiApp schedule, and transfer payload fixed. In A, evaluate parent n_e with the baseline two-term FV boundary expansion. In B, disable two-term boundary expansion only for the operational parent n_e while an immutable transferred snapshot remains two-term. Record plasma_electrode side extrema and require the original negative heavy_transport signature to appear only in A.

Intentional boundary-face A/B/C follow-up: incorporate the completed CodeRabbit review and Run #34 evidence. Probe the complete r31_plasma_all_boundary. A keeps two-term evaluation on both transferred mirrors; B keeps the Run #34 parent-only one-term control; C sets one-term boundary evaluation on parent n_e and child n_e_heavy_frozen only, while live solved child n_e remains two-term. Require exact child transport/wall-loss/SEE bindings, preserve the parent two-term snapshot control, and verify the old negative heavy_transport signature migrates from parent to child in B and disappears only in C while C progresses beyond B.
